# Goal 005：分支命名规范与工作流可发现性

- 状态：in progress
- GitHub Issue：[#3](https://github.com/formal-you/validation-gen/issues/3)
- 创建：2026-08-22

## 目标

把分支命名规范显式化并在 CI 中强制校验，同时让 AGENTS.md 能直接跳转到开发工作流文档，
使仓库约定可发现、可执行。

## 范围

- `docs/gitops/README.md` 分支命名规范；
- `.github/workflows/ci.yml` 新增 `branch-name` 作业；
- `AGENTS.md` 顶部工作流跳转 + 文档流约定；
- `CONTRIBUTING.md`、`CHANGELOG.md` 同步；
- 本地镜像 `docs/issue/005`、`docs/goal/005`、`docs/pr/005`。

## 完成定义（DoD）

1. 分支命名规范 `<type>/<NNN>-<kebab-slug>` 写入文档；
2. CI `branch-name` 作业校验分支名，且本 PR 分支 `docs/005-branch-name-spec` 通过；
3. `AGENTS.md` 可跳转查看 `docs/gitops/README.md`；
4. 本变更通过 GitHub Issue #3 + PR（`gh pr merge --rebase`）合并，main 保持线性；
5. 本地 `main` 与 `origin/main` 同步。
