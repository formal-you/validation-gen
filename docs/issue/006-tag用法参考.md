# Issue 006：validate/default tag 用法参考文档

- 状态：open
- GitHub Issue：[#5](https://github.com/formal-you/validation-gen/issues/5)
- 创建：2026-08-22

## 背景

README 有规则白名单，但没有一份专门讲「validate tag 支持哪些校验、如何书写」的文档：
分隔符（逗号 vs 空格）、规则参数格式、适用类型、default 写法、常见报错等都需要说清楚，
且必须与 `pkg/parser` 实现一一对应。

## 需求

1. 新增 `docs/tag-reference.md`：
   - 规则之间用逗号 `,` 分隔、规则名与参数用 `=`、oneof 候选项用空格（含 `'...'` 单引号 token）；
   - 11 条白名单规则的参数与适用类型表；
   - 支持类型（标量 + named type + 一级指针）；
   - `default` tag 写法；
   - 生成期报错的常见错误表；
   - 语义要点（首个失败规则、pointer omitempty、UTF-8 字符数等）。
2. README 文档入口与规则白名单章节增加指向该文档的链接。

## 验收

- [ ] `docs/tag-reference.md` 与 `pkg/parser` 实现一致（分隔符、参数、类型约束）；
- [ ] README 可跳转到 `docs/tag-reference.md`；
- [ ] 本变更通过 GitHub Issue #5 + PR（rebase 合并，main 保持线性）。
