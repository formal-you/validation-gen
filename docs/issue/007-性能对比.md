# Issue 007：性能对比（生成代码 vs validator/v10）

- 状态：open
- GitHub Issue：[#7](https://github.com/formal-you/validation-gen/issues/7)
- 创建：2026-08-22

## 背景

README 缺少与 go-playground/validator/v10 的性能对比。需要补充基准测试并说明：
即使 validator 使用 map 缓存解析后的反射对象并构建校验函数，代码生成依旧更快、
更好阅读、更好修改。

## 需求

1. `example/dto` 增加基准测试（`bench_test.go`）：同一 DTO、同一输入，对比
   生成 `Validate()`（Static）、复用预热 validator 实例（ValidatorWarm）、
   `runtime.Validate(ctx, nil, v)`（RuntimeFallback）三条路径，输出 ns/op、B/op、allocs/op；
2. 运行 `go test -bench . -benchmem ./example/dto/` 并记录结果；
3. README 增加「性能对比」章节：基准结果表 + validator 缓存机制 vs 代码生成
   的零反射/零缓存查找说明 + 使用边界。

## 验收

- [ ] `go test -bench . -benchmem ./example/dto/` 可运行；
- [ ] README 含性能对比表与机制说明；
- [ ] 本变更通过 GitHub Issue #7 + PR（rebase 合并，main 保持线性）。
