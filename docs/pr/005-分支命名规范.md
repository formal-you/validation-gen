# PR 005：分支命名规范（CI 校验）与 AGENTS.md 工作流跳转

- 状态：merged（GitHub rebase 合并，main 保持线性）
- 分支：`docs/005-branch-name-spec` -> `main`
- GitHub PR：[#4](https://github.com/formal-you/validation-gen/pull/4)
- 关联 Issue：[#3](https://github.com/formal-you/validation-gen/issues/3)（005 分支命名规范）

## 摘要

把分支命名规范显式化并写入 CI 强制校验，同时让 AGENTS.md 能直接跳转查看开发工作流文档，
使仓库约定可发现、可执行。

## 变更清单（已实现）

- `docs/gitops/README.md`：定义分支命名规范 `<type>/<NNN>-<kebab-slug>`
  （type ∈ `feat`/`fix`/`refactor`/`docs`/`test`/`chore`，NNN 为 issue 编号）；
- `.github/workflows/ci.yml`：新增 `branch-name` 作业，PR 时强制校验分支名
  （正则 `^(feat|fix|refactor|docs|test|chore)/[0-9]+(-[a-z0-9]+)+$`）；
- `AGENTS.md`：顶部增加开发工作流文档（`docs/gitops/README.md`）跳转链接，
  文档流（gitOps）约定补充分支命名；
- `CONTRIBUTING.md` / `CHANGELOG.md` 同步分支命名规范；
- 本地镜像：`docs/issue/005`、`docs/goal/005`、`docs/pr/005`
  （关联 GitHub Issue #3 / PR #4）。

## 验收结果（2026-08-22）

- [x] `docs/gitops/README.md` 含分支命名规范
- [x] CI 的 `branch-name` 作业校验分支名，且本 PR 分支 `docs/005-branch-name-spec` 通过
- [x] `AGENTS.md` 可点击跳转到 `docs/gitops/README.md`
- [x] 本 PR 通过 `gh pr merge --rebase` 合并，main 保持线性
