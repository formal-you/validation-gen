# PR 006：validate/default tag 用法参考文档

- 状态：merged（GitHub rebase 合并，main 保持线性）
- 分支：`docs/006-tag-reference` -> `main`
- GitHub PR：[#6](https://github.com/formal-you/validation-gen/pull/6)
- 关联 Issue：[#5](https://github.com/formal-you/validation-gen/issues/5)（006 tag 用法参考）

## 摘要

新增一份专门讲「validate tag 支持哪些校验、如何书写」的参考文档，分隔符、参数格式、
类型约束均与 `pkg/parser` 实现一一对应；README 增加跳转链接。

## 变更清单（已实现）

- 新增 `docs/tag-reference.md`：分隔符速查（规则用逗号、参数用 `=`、oneof 用空格、
  含空格的候选项用 `'...'`）、11 条白名单规则表、支持类型、default 写法、
  生成期常见报错表、语义要点；
- README：规则白名单章节顶部与文档入口表增加指向 `docs/tag-reference.md` 的链接；
- 本地镜像：`docs/issue/006`、`docs/goal/006`、`docs/pr/006`
  （关联 GitHub Issue #5 / PR #6）。

## 验收结果（2026-08-22）

- [x] 文档与 `pkg/parser` 实现一致（已逐条核对：`strings.Split(tag, ",")`、
      `=` 分隔参数、`splitOneOf` 空格/单引号、`check.Runes` UTF-8 字符数）；
- [x] README 可跳转到 `docs/tag-reference.md`；
- [x] 本 PR 通过 `gh pr merge --rebase` 合并，main 保持线性。
