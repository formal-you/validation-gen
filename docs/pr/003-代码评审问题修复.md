# PR 003：代码评审问题修复

- 状态：merged（本地 gitOps 闭环 + 远程推送）
- 分支：`fix/003-review-fixes` -> `main`
- 关联 Issue：`docs/issue/003-代码评审问题修复.md`
- 关联评审：`docs/review/2026-08-22-代码评审报告.md`

## 摘要

按评审报告（Standards/Spec 均 8/10）修复发现的问题：

- **注释一致性**：`example/grpc/grpc.go` 明确「结构化信息需在转换前通过
  `req.Validate()` 获取」；`example/dto/dto.go` 说明无 default 字段不生成 `FillDefaults()`。
- **生成器行为**：`pkg/gen` 只为含 `default` 字段的类型生成 `FillDefaults()`，
  消除无意义空方法（CreateUserRequest/UserProfile/Order/NoValidate 不再生成）。
- **测试补齐**：`cmd/valgen` 新增「非法规则不写文件」集成测试（`dive` fixture）；
  `example/http` 新增「显式提供零值」用例（零值不被当作未提供跳过）。
- **治理与文档**：提交 `AGENTS.md` 与评审报告；README「三分钟跑起来」改用含
  default 的 `Settings` 示例，与代码行为一致。
- **可测试性**：`cmd/valgen` 拆出 `run()`，便于集成测试验证退出码与不写文件行为；
  顺带重命名 `useerr` -> `useErrors`。

## 验收结果（2026-08-22）

- [x] `go generate ./...` 通过，生成文件再生成无 diff
- [x] `go build ./...` / `go vet ./...` 通过
- [x] `go test ./...` / `go test -race ./...` 全绿
- [x] `git diff --check` 无空白错误
- [x] `TestInvalidRulesDoNotOverwrite`：含 `dive` 的 fixture 生成失败且不覆盖已有文件
- [x] `TestCreateUserHandler` 新增显式零值用例（nickname 空串 -> min、age=0 -> 204）
- [x] 已推送到 `origin/main`
