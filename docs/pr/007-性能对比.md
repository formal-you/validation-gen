# PR 007：生成代码 vs validator/v10 性能基准与 README 更新

- 状态：merged（GitHub rebase 合并，main 保持线性）
- 分支：`test/007-validator-benchmark` -> `main`
- GitHub PR：[#8](https://github.com/formal-you/validation-gen/pull/8)
- 关联 Issue：[#7](https://github.com/formal-you/validation-gen/issues/7)（007 性能对比）

## 摘要

新增性能基准（生成代码 vs go-playground/validator/v10），并在 README 说明：
即使 validator 使用 map 缓存解析后的反射对象并构建校验函数，代码生成依旧更快、
更好阅读、更好修改。

## 变更清单（已实现）

- `example/dto/bench_test.go`：同一 DTO/输入对比三条路径
  （Static 生成代码 / ValidatorWarm 预热实例 / RuntimeFallback 每次新建实例），
  覆盖合法与非法（多错误）输入，输出 ns/op、B/op、allocs/op；
- README「⚡ 性能对比」章节：基准结果表 + validator 缓存机制 vs 生成代码
  的零反射/零缓存查找说明 + 何时用 runtime 的边界；
- 本地镜像：`docs/issue/007`、`docs/goal/007`、`docs/pr/007`
  （关联 GitHub Issue #7 / PR #8）。

## 基准摘要（Go 1.26.1 / linux amd64 / i7-14700K）

| 场景 | Static | ValidatorWarm | RuntimeFallback |
| --- | --- | --- | --- |
| CreateUserRequest 合法 | 815 ns/op | 1,612 ns/op | 25,306 ns/op |
| CreateUserRequest 非法 | 835 ns/op | 2,131 ns/op | 29,022 ns/op |
| Settings 合法 | 15 ns/op | 431 ns/op | — |

## 验收结果（2026-08-22）

- [x] `go test -bench . -benchmem ./example/dto/` 可运行；
- [x] `go build ./...` / `go vet ./...` / `go test ./...` 全绿，`git diff --check` 通过；
- [x] README 含性能对比表与机制说明；
- [x] 本 PR 通过 `gh pr merge --rebase` 合并，main 保持线性。
