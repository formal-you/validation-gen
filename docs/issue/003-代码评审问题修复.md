# Issue 003：代码评审问题修复

- 状态：open
- 创建：2026-08-22
- 关联评审：`docs/review/2026-08-22-代码评审报告.md`（Standards 8/10、Spec 8/10）

## 背景

对根提交以来的全部代码做了双轴评审（Standards + Spec），两轴均 8/10。
评审发现 1 处硬性注释违规、2 处文档/代码不一致与 2 个缺失的自动化测试，
需要修复后走 gitOps 闭环并推送到远程。

## 需求

1. 修正 `example/grpc/grpc.go` 误导性注释：`status.Error` 会抹掉错误结构，
   结构化信息需在转换前通过 `req.Validate()` 返回的 error 调用 `errorx.CollectFieldErrors` 获取。
2. 生成器只为含 `default` 字段的类型生成 `FillDefaults()`：
   与 `dto.go` 的 NoValidate 注释一致（该类型不生成），并消除无意义空方法噪音。
3. 补「非法规则不写文件」自动化测试：`cmd/valgen` 集成测试，
   含 runtime-only 规则（`dive`）的 fixture 生成失败且不覆盖已有生成文件。
4. `example/http` 补「显式提供零值」用例：零值不会被当作未提供跳过。
5. 提交 `AGENTS.md` 与 `docs/review/2026-08-22-代码评审报告.md`（当前未跟踪）。
6. README「三分钟跑起来」示例改为含 `default` 字段的 `Settings`，与代码行为一致。

## 验收

- `go generate ./...`、`go build ./...`、`go vet ./...`、`go test ./...`、`go test -race ./...` 全绿；
- 生成文件再生成无 diff；
- 新增「非法规则不写文件」与「显式提供零值」测试通过；
- gitOps 闭环：issue 003 → PR 003 合并 → 推送到远程（origin/main）。
