# Goal 006：validate/default tag 用法参考

- 状态：in progress
- GitHub Issue：[#5](https://github.com/formal-you/validation-gen/issues/5)
- 创建：2026-08-22

## 目标

让使用者不需要读源码就能正确书写 `validate:"..."` / `default:"..."` tag：
明确支持哪些规则、参数怎么写、逗号与空格的边界、常见报错，并保证文档与实现一致。

## 范围

- 新增 `docs/tag-reference.md`；
- README 文档入口与规则白名单章节增加链接；
- 本地镜像 `docs/issue/006`、`docs/goal/006`、`docs/pr/006`。

## 完成定义（DoD）

1. `docs/tag-reference.md` 的分隔符、参数格式、类型约束与 `pkg/parser` 一致；
2. README 可跳转到该文档；
3. 本变更通过 GitHub Issue #5 + PR（`gh pr merge --rebase`）合并，main 保持线性；
4. 本地 `main` 与 `origin/main` 同步。
