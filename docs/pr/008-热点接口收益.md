# PR 008：README 性能对比补充热点接口收益分析

- 状态：merged（GitHub rebase 合并，main 保持线性）
- 分支：`docs/008-hotpath-benefit` -> `main`
- GitHub PR：[#10](https://github.com/formal-you/validation-gen/pull/10)
- 关联 Issue：[#9](https://github.com/formal-you/validation-gen/issues/9)（008 热点接口收益）

## 摘要

在 README「⚡ 性能对比」章节补充「热点接口：收益值得关注」小节，用仓库自己的基准数字
量化热点路径上代码生成的收益，并说明独立生成器形态的合理性。

## 变更清单（已实现）

- README 新增小节「热点接口：收益值得关注」：
  - ValidatorWarm 对比每请求节省：合法 ~797 ns、非法 ~1.3 µs 且 allocs 17→29；
  - 按 RPS 换算单核收益：10k RPS ≈0.8%、100k RPS ≈8% 单核，分配约减半、GC 压力更小；
  - 说明热点团队直接采用独立生成器：gin 手动调用 `Validate()`、
    go-zero 实现 `Validate() error` 接口由 `httpx.Parse` 自动调用；
- 本地镜像：`docs/issue/008`、`docs/goal/008`、`docs/pr/008`
  （关联 GitHub Issue #9 / PR #10）。

## 验收结果（2026-08-23）

- [x] README 小节数字与 `example/dto/bench_test.go` 基准一致；
- [x] 本 PR 通过 `gh pr merge --rebase` 合并，main 保持线性。
