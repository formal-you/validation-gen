# Goal 007：性能对比基准与 README 更新

- 状态：in progress
- GitHub Issue：[#7](https://github.com/formal-you/validation-gen/issues/7)
- 创建：2026-08-22

## 目标

用基准测试量化「生成代码 vs validator/v10」，并在 README 说明：
即使 validator 有 map 缓存 + 已构建校验函数，代码生成依旧更快、更好阅读、更好修改。

## 范围

- `example/dto/bench_test.go`（Static / ValidatorWarm / RuntimeFallback 三路径）；
- README「性能对比」章节（结果表 + 机制说明 + 边界）；
- 本地镜像 `docs/issue/007`、`docs/goal/007`、`docs/pr/007`。

## 完成定义（DoD）

1. 基准测试通过 `go vet` / `go test` 编译检查，且 `-bench` 可运行；
2. README 含性能对比表与机制说明；
3. 本变更通过 GitHub Issue #7 + PR（`gh pr merge --rebase`）合并，main 保持线性；
4. 本地 `main` 与 `origin/main` 同步。
